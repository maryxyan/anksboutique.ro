name: 🚀 Build & Deploy to anksboutique.ro

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: 📦 Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: latest

      - name: 📦 Setup Node 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: 📥 Install dependencies
        run: pnpm install

      - name: 🔨 Build frontend
        run: |
          cd artifacts/ank-boutique
          PORT=300 BASE_PATH=/ pnpm build
          echo "✅ Frontend built"

      - name: � Prepare deploy bundle
        run: |
          rm -rf deploy
          mkdir -p deploy
          cp -R public_html/. deploy/          rm -f deploy/api-health-check.php          rm -rf deploy/assets deploy/index.html
          cp -r artifacts/ank-boutique/dist/assets deploy/assets
          cp artifacts/ank-boutique/dist/index.html deploy/index.html
          echo "✅ Prepared deploy bundle"

      - name: 📂 Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: deploy/
          server-dir: public_html/
          exclude: |
            **/.gitkeep
            **/error_log
            **/api-health-check.php
