- name: 🚩 Create restart flag
  run: |
    mkdir -p deploy
    cat > deploy/restart.flag <<EOF
    {
      "time":"$(date -u +"%Y-%m-%dT%H:%M:%SZ")",
      "commit":"${{ github.sha }}",
      "run":"${{ github.run_number }}"
    }
    EOF
